{
  "dependencies": {
    "json-server": "^0.17.0"
  },
  "scripts": {
    "start": "json-server -p 8080 db.json -m ./node_modules/json-server-auth -r routes.json"
  },
  "devDependencies": {
    "json-server-auth": "^2.1.0"
  }
}
